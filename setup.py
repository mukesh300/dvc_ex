from setuptools import setup, find_packages

setup(
    name="src",
    version="0.1",
    description="it's a wine Q package",
    author="mukesh300",
    packages=find_packages(),
    license="MIT"
)